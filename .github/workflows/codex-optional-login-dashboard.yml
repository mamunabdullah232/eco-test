name: Apply optional login dashboard update

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/codex-optional-login-dashboard.yml

permissions:
  contents: write

jobs:
  patch-site:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch site files
        shell: python
        run: |
          from pathlib import Path

          def replace_once(text, old, new, label):
              if old not in text:
                  print(f"skip: {label}")
                  return text
              return text.replace(old, new, 1)

          index_path = Path("index.html")
          index = index_path.read_text(encoding="utf-8")
          index = index.replace('.nav-link.mock{--accent:var(--rose)}\n', '')
          index = replace_once(index, '''.home-shell{
  display:grid;
  place-items:center;
  min-height:calc(100vh - 170px);
}
''', '''.home-shell{
  display:grid;
  place-items:center;
  min-height:calc(100vh - 170px);
  gap:20px;
}
''', 'home shell gap')
          index = replace_once(index, '''.fact-copy p{
  color:#334155;
  font-size:1.03rem;
}
''', '''.fact-copy p{
  color:#334155;
  font-size:1.03rem;
}
.home-dashboard{
  width:min(920px,100%);
  display:grid;
  grid-template-columns:repeat(2,minmax(0,1fr));
  gap:16px;
}
.home-login-card{
  display:grid;
  gap:8px;
  min-height:138px;
  padding:22px;
  border:1px solid var(--border);
  border-radius:18px;
  background:#fff;
  box-shadow:0 10px 26px rgba(15,23,42,.065);
  border-top:6px solid var(--blue);
}
.home-login-card.admin{border-top-color:var(--violet)}
.home-login-card strong{
  color:var(--ink);
  font-size:1.12rem;
  line-height:1.25;
}
.home-login-card p{
  color:var(--muted);
  font-size:.94rem;
}
.home-login-card span{
  justify-self:start;
}
''', 'home dashboard css')
          index = index.replace('  .fact-card{grid-template-columns:1fr}\n', '  .fact-card,.home-dashboard{grid-template-columns:1fr}\n')
          index = index.replace('      <a class="nav-link mock nav-route" href="#mock-tests" data-page="mock-tests"><span class="bi">Mock Test<span class="as">মক টেষ্ট</span></span></a>\n', '')
          index = replace_once(index, '''      </article>
    </div>
  </section>
''', '''      </article>
      <div class="home-dashboard" aria-label="Login dashboard">
        <a class="home-login-card" href="/login.html?redirect=/student-dashboard.html">
          <strong>Student Login / শিক্ষাৰ্থী লগইন</strong>
          <p>Optional login for saving English Grammar mock-test attempts and viewing previous results.</p>
          <span class="badge green">View My Results / ফলাফল চাওক</span>
        </a>
        <a class="home-login-card admin" href="/login.html?redirect=/admin-dashboard.html">
          <strong>Admin Login / এডমিন লগইন</strong>
          <p>View student participation, scores, submitted dates, and answer details from the dashboard.</p>
          <span class="badge">Open Admin Dashboard / এডমিন খোলক</span>
        </a>
      </div>
    </div>
  </section>
''', 'home dashboard cards')
          index = index.replace('''        <a class="student-action" href="/login.html">
          <strong>Student Login</strong>
          <p>Sign in with Google to take protected English Grammar tests and save results.</p>
          <span class="badge green">Login / Dashboard</span>
        </a>
''', '')
          index_path.write_text(index, encoding="utf-8")

          mock_path = Path("mock-test.html")
          mock = mock_path.read_text(encoding="utf-8")
          mock = mock.replace('if(!isEnglishGrammarTest() || typeof window.saveXohopathiAttempt !== "function") return;', 'if(typeof window.saveXohopathiAttempt !== "function") return;')
          mock = mock.replace('    await window.saveXohopathiAttempt({', '    const saveInfo = await window.saveXohopathiAttempt({', 1)
          mock = mock.replace('''    if(resultBox){
      resultBox.insertAdjacentHTML("beforeend", "<br><span>Attempt saved to dashboard.</span>");
''', '''    if(resultBox && saveInfo && saveInfo.saved){
      resultBox.insertAdjacentHTML("beforeend", "<br><span>Attempt saved to dashboard.</span>");
''', 1)
          mock_path.write_text(mock, encoding="utf-8")

          Path("firebase-config.js").write_text('''// Xohopathi Firebase configuration and shared Firebase exports.
// Uses Firebase CDN modules so the static website can run without a build step.

import { initializeApp } from "https://www.gstatic.com/firebasejs/12.13.0/firebase-app.js";
import { getAnalytics } from "https://www.gstatic.com/firebasejs/12.13.0/firebase-analytics.js";
import {
  getAuth,
  GoogleAuthProvider,
  signInWithPopup,
  signInWithRedirect,
  getRedirectResult,
  onAuthStateChanged,
  signOut
} from "https://www.gstatic.com/firebasejs/12.13.0/firebase-auth.js";
import {
  getFirestore,
  collection,
  doc,
  setDoc,
  getDoc,
  getDocs,
  addDoc,
  query,
  where,
  serverTimestamp
} from "https://www.gstatic.com/firebasejs/12.13.0/firebase-firestore.js";

const firebaseConfig = {
  apiKey: "AIzaSyCYp6u2VYlaFwCGGpbZ05PxNwFuAwIF-Xg",
  authDomain: "xohopathi.firebaseapp.com",
  projectId: "xohopathi",
  storageBucket: "xohopathi.firebasestorage.app",
  messagingSenderId: "194640996200",
  appId: "1:194640996200:web:71c6ec023414a41e2b98dd",
  measurementId: "G-5VQC8RR8LC"
};

export const ADMIN_EMAIL = "mamunabdullah262@gmail.com";

export const app = initializeApp(firebaseConfig);

let analytics = null;
try {
  analytics = getAnalytics(app);
} catch (error) {
  console.warn("Firebase Analytics not initialized in this environment:", error);
}
export { analytics };

export const auth = getAuth(app);
auth.useDeviceLanguage();

export const googleProvider = new GoogleAuthProvider();
export const db = getFirestore(app);

export {
  signInWithPopup,
  signInWithRedirect,
  getRedirectResult,
  onAuthStateChanged,
  signOut,
  collection,
  doc,
  setDoc,
  getDoc,
  getDocs,
  addDoc,
  query,
  where,
  serverTimestamp
};
''', encoding="utf-8")

          Path("login.html").write_text('''<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Xohopathi Login</title>
  <style>
    :root{--primary:#2563eb;--dark:#0f172a;--muted:#64748b;--bg:#f8fafc;--card:#fff;--border:#e2e8f0}
    *{box-sizing:border-box}
    body{margin:0;min-height:100vh;font-family:Arial,sans-serif;background:linear-gradient(135deg,#eff6ff,#f8fafc);color:var(--dark);display:flex;align-items:center;justify-content:center;padding:24px}
    .card{width:100%;max-width:420px;background:var(--card);border:1px solid var(--border);border-radius:22px;padding:32px;box-shadow:0 20px 45px rgba(15,23,42,.12);text-align:center}
    .logo{width:64px;height:64px;border-radius:18px;display:inline-grid;place-items:center;background:#dbeafe;color:var(--primary);font-size:30px;font-weight:900;margin-bottom:14px}
    h1{margin:0 0 8px;font-size:28px}
    p{margin:0 0 24px;color:var(--muted);line-height:1.5}
    button{width:100%;border:0;border-radius:14px;padding:14px 18px;font-size:16px;font-weight:700;cursor:pointer;background:var(--primary);color:white}
    button:hover{background:#1d4ed8}
    button:disabled{opacity:.65;cursor:wait}
    .status{min-height:22px;margin-top:16px;color:#b91c1c;font-size:14px}
    .home{display:inline-block;margin-top:18px;color:var(--primary);text-decoration:none;font-weight:700}
  </style>
</head>
<body>
  <main class="card">
    <div class="logo">স</div>
    <h1>Login to Xohopathi</h1>
    <p>Students can save and view test results. Admin can view participation and scores.</p>
    <button id="googleLoginBtn">Continue with Google</button>
    <div class="status" id="statusText"></div>
    <a class="home" href="/index.html">Back to Home</a>
  </main>
  <script type="module" src="/login.js"></script>
</body>
</html>
''', encoding="utf-8")

          Path("login.js").write_text('''import {
  auth,
  db,
  googleProvider,
  signInWithRedirect,
  getRedirectResult,
  doc,
  setDoc,
  serverTimestamp,
  ADMIN_EMAIL
} from "./firebase-config.js";

const googleLoginBtn = document.getElementById("googleLoginBtn");
const statusText = document.getElementById("statusText");

function getRedirectUrl() {
  const params = new URLSearchParams(window.location.search);
  return params.get("redirect") || sessionStorage.getItem("xohopathiLoginRedirect") || "";
}

function rememberRedirect() {
  const params = new URLSearchParams(window.location.search);
  const redirect = params.get("redirect") || "";
  if (redirect) sessionStorage.setItem("xohopathiLoginRedirect", redirect);
}

async function saveUser(user) {
  const email = (user.email || "").toLowerCase();
  const role = email === ADMIN_EMAIL.toLowerCase() ? "admin" : "student";
  await setDoc(doc(db, "users", user.uid), {
    uid: user.uid,
    name: user.displayName || "",
    email,
    photoURL: user.photoURL || "",
    role,
    provider: "google",
    lastLoginAt: serverTimestamp(),
    updatedAt: serverTimestamp()
  }, { merge: true });
  return role;
}

function finishLogin(role) {
  const redirect = getRedirectUrl();
  sessionStorage.removeItem("xohopathiLoginRedirect");
  window.location.href = redirect || (role === "admin" ? "/admin-dashboard.html" : "/student-dashboard.html");
}

rememberRedirect();

getRedirectResult(auth)
  .then(async (result) => {
    if (!result || !result.user) return;
    statusText.textContent = "Login successful. Opening dashboard...";
    const role = await saveUser(result.user);
    finishLogin(role);
  })
  .catch((error) => {
    console.error(error);
    statusText.textContent = "Login failed. Please check Google sign-in and authorized domains.";
    googleLoginBtn.disabled = false;
  });

googleLoginBtn.addEventListener("click", async () => {
  statusText.textContent = "Opening Google sign-in...";
  googleLoginBtn.disabled = true;
  try {
    rememberRedirect();
    await signInWithRedirect(auth, googleProvider);
  } catch (error) {
    console.error(error);
    statusText.textContent = "Login failed. Please check Google sign-in and authorized domains.";
    googleLoginBtn.disabled = false;
  }
});
''', encoding="utf-8")

          Path("student-dashboard.html").write_text('''<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Xohopathi Student Dashboard</title>
  <style>
    :root{--primary:#2563eb;--dark:#0f172a;--muted:#64748b;--bg:#f8fafc;--card:#fff;--border:#e2e8f0}
    *{box-sizing:border-box}
    body{margin:0;font-family:Arial,sans-serif;background:var(--bg);color:var(--dark)}
    header{background:#fff;border-bottom:1px solid var(--border);padding:18px 24px;display:flex;justify-content:space-between;align-items:center;gap:16px}
    h1,h2{margin:0}
    main{max-width:1100px;margin:0 auto;padding:28px 18px}
    .grid{display:grid;grid-template-columns:1fr;gap:18px}
    .card{background:var(--card);border:1px solid var(--border);border-radius:18px;padding:22px;box-shadow:0 10px 28px rgba(15,23,42,.06)}
    .muted{color:var(--muted)}
    .btn{display:inline-block;border:0;border-radius:12px;padding:11px 14px;background:var(--primary);color:white;text-decoration:none;font-weight:700;cursor:pointer}
    .btn.secondary{background:#e2e8f0;color:var(--dark)}
    table{width:100%;border-collapse:collapse;margin-top:14px}
    th,td{text-align:left;border-bottom:1px solid var(--border);padding:10px 8px;font-size:14px}
    th{color:var(--muted)}
    .actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:14px}
  </style>
</head>
<body>
  <header>
    <div><h1>Student Dashboard</h1><div class="muted" id="studentInfo">Loading...</div></div>
    <div class="actions">
      <a class="btn secondary" href="/index.html">Home</a>
      <button class="btn secondary" id="logoutBtn">Logout</button>
    </div>
  </header>
  <main class="grid">
    <section class="card"><h2>My Previous Attempts</h2><p class="muted">Results are saved here when you are logged in before submitting a mock test.</p><div id="attemptsArea" class="muted">Loading attempts...</div></section>
  </main>
  <script type="module" src="/student-dashboard.js"></script>
</body>
</html>
''', encoding="utf-8")

          Path("save-attempt.js").write_text('''// Saves Xohopathi mock-test attempts to Firestore when a student is logged in.
// Tests remain open to everyone; login is only needed for saving/viewing results.

import { auth, db, onAuthStateChanged, collection, addDoc, serverTimestamp } from "./firebase-config.js";

let currentUser = null;
let authResolved = false;
let resolveAuthReady;
const authReady = new Promise((resolve) => {
  resolveAuthReady = resolve;
});

onAuthStateChanged(auth, (user) => {
  currentUser = user;
  authResolved = true;
  resolveAuthReady(user);
});

window.saveXohopathiAttempt = async function saveXohopathiAttempt({
  className = "10",
  subject = "English",
  topic = "Grammar",
  testId = "",
  testTitle = "",
  score = 0,
  totalQuestions = 0,
  answers = [],
  correctAnswers = [],
  extra = {}
}) {
  const user = authResolved ? currentUser : await authReady;
  if (!user) {
    return { saved: false, reason: "not-logged-in" };
  }

  const attempt = {
    uid: user.uid,
    studentName: user.displayName || "",
    email: (user.email || "").toLowerCase(),
    className,
    subject,
    topic,
    testId,
    testTitle,
    score,
    totalQuestions,
    answers,
    correctAnswers,
    extra,
    submittedAt: serverTimestamp(),
    userAgent: navigator.userAgent
  };

  const docRef = await addDoc(collection(db, "testAttempts"), attempt);
  return { saved: true, id: docRef.id };
};
''', encoding="utf-8")

          Path(".github/workflows/codex-optional-login-dashboard.yml").unlink(missing_ok=True)

      - name: Validate and commit
        shell: bash
        run: |
          git diff --check
          git config user.name "codex-bot"
          git config user.email "codex-bot@users.noreply.github.com"
          git add index.html mock-test.html firebase-config.js login.html login.js student-dashboard.html save-attempt.js .github/workflows/codex-optional-login-dashboard.yml
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Make login dashboard optional"
            git push
          fi
